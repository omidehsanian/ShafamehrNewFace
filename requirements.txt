# وابستگی‌های نسخه وب‌محور (رابط کاربری مدرن با pywebview)
#   pip install -r requirements.txt
pywebview>=5.0
openpyxl>=3.1
Pillow>=10.0
cryptography>=42.0

# فقط برای «ساخت فایل exe»:
pyinstaller>=6.0
